{
  "name": "@beneverk/opencode-remote-fleet-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker for Opencode Remote Fleet — aggregator dashboard, registry, per-machine transparent proxy, and notification hub.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "wrangler": "^4.113.0",
    "typescript": "^7.0.0",
    "@cloudflare/workers-types": "^5.0.0",
    "vitest": "^4.1.10",
    "@cloudflare/vitest-pool-workers": "^0.18.8"
  }
}
